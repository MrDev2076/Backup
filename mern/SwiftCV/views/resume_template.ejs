<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Generated Resume</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
    <div class="container mt-5">
        <!-- Header Section -->
        <div class="text-center">
            <h1>{{name}}</h1>
            <p><strong>Contact:</strong> {{contact}}</p>
          <!--- {% if profile_pic %}
                <img src="{{profile_pic}}" alt="Profile Picture" class="img-thumbnail rounded-circle" width="150" height="150">
            {% endif %} -->
            <hr>
        </div>

        <!-- Summary Section -->
        {% if summary %}
            <h4>Summary</h4>
            <p>{{summary}}</p>
        {% endif %}

        <!-- Objective Section -->
        {% if objective %}
            <h4>Objective</h4>
            <p>{{objective}}</p>
        {% endif %}

        <!-- Education Section -->
        {% if education %}
            <h4>Education</h4>
            <p>{{education}}</p>
        {% endif %}

        <!-- Experience Section -->
        {% if experience %}
            <h4>Experience</h4>
            <p>{{experience}}</p>
        {% endif %}

        <!-- Skills Section -->
        {% if skills %}
            <h4>Skills</h4>
            <p>{{skills}}</p>
        {% endif %}

        <!-- Certifications Section -->
        {% if certifications %}
            <h4>Certifications</h4>
            <p>{{certifications}}</p>
        {% endif %}

        <!-- Achievements Section -->
        {% if achievements %}
            <h4>Achievements</h4>
            <p>{{achievements}}</p>
        {% endif %}

        <!-- Projects Section -->
        {% if projects %}
            <h4>Projects</h4>
            <p>{{projects}}</p>
        {% endif %}

        <!-- Publications Section -->
        {% if publications %}
            <h4>Publications</h4>
            <p>{{publications}}</p>
        {% endif %}

        <!-- Languages Section -->
        {% if languages %}
            <h4>Languages</h4>
            <p>{{languages}}</p>
        {% endif %}

        <!-- Awards Section -->
        {% if awards %}
            <h4>Awards</h4>
            <p>{{awards}}</p>
        {% endif %}

        <!-- Hobbies Section -->
        {% if hobbies %}
            <h4>Hobbies</h4>
            <p>{{hobbies}}</p>
        {% endif %}

        <!-- Interests Section -->
        {% if interests %}
            <h4>Interests</h4>
            <p>{{interests}}</p>
        {% endif %}

        <!-- Volunteer Work Section -->
        {% if volunteer %}
            <h4>Volunteer Work</h4>
            <p>{{volunteer}}</p>
        {% endif %}

        <!-- References Section -->
        {% if references %}
            <h4>References</h4>
            <p>{{references}}</p>
        {% endif %}

        <!-- Portfolio Section -->
        {% if portfolio %}
            <h4>Portfolio</h4>
            <p><a href="{{portfolio}}" target="_blank">{{portfolio}}</a></p>
        {% endif %}

        <!-- Trainings Section -->
        {% if trainings %}
            <h4>Trainings</h4>
            <p>{{trainings}}</p>
        {% endif %}

        <!-- Workshops Section -->
        {% if workshops %}
            <h4>Workshops</h4>
            <p>{{workshops}}</p>
        {% endif %}

        <!-- Social Media Links Section -->
        {% if gitlink %}
            <h4>GitHub</h4>
            <p><a href="{{gitlink}}" target="_blank">{{gitlink}}</a></p>
        {% endif %}
        {% if linkedin %}
            <h4>LinkedIn</h4>
            <p><a href="{{linkedin}}" target="_blank">{{linkedin}}</a></p>
        {% endif %}
        {% if social_media %}
            <h4>Other Social Media</h4>
            <p>{{social_media}}</p>
        {% endif %}

        <!-- Footer -->
        <footer class="text-center mt-4">
            <p>Generated by SwiftCV</p>
        </footer>
    </div>

    <!-- Bootstrap JS and dependencies (optional) -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
